# System Dependencies for Manual Aptos Setup
# Install with: sudo apt install -y $(cat requirements.txt)

# Build tools
binutils
lld

# Development libraries
libudev-dev
libdw-dev
pkg-config

# Additional dependencies that may be needed
# (uncomment if you encounter build errors)
# build-essential
# cmake
# git
# curl
# wget
